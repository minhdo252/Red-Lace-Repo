{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "startCommand": "(python -u -m app.agent.seed_scam_patterns && python -u -m app.agent.seed_price_references) & exec python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
